# lammps-colab wheel (serial)

Prebuilt serial (CPU-only, no MPI) LAMMPS Python wheels.

## Install
```bash
pip install https://github.com/<USER>/lammps-colab-wheels/releases/download/v0.0.0/lammps_colab-0.0.0-cp312-cp312-linux_x86_64.whl
